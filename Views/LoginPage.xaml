<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Zenimint_Funds.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Zenimint_Funds"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300" Spacing="15">

            <TextBlock Text="Bienvenido a FinanzasApp" 
                       FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>

            <TextBox x:Name="txtUsuario" Header="Usuario" PlaceholderText="Ingresa tu usuario"/>

            <PasswordBox x:Name="txtPassword" Header="Contraseña" 
                         PlaceholderText="••••••" KeyDown="txtPassword_KeyDown"/>

            <Button x:Name="btnLogin" Content="Iniciar Sesión" 
                    Click="btnLogin_Click" HorizontalAlignment="Stretch"/>
            
            <Button x:Name="btnHello" Content="Windows Hello"
                    Click="btnHello_Click" HorizontalAlignment="Stretch"
                    Style="{StaticResource AccentButtonStyle}" Margin="0,10,0,0"/>

                <TextBlock x:Name="lblError" Foreground="Red" 
                       HorizontalAlignment="Center" Visibility="Collapsed"/>

        </StackPanel>
    </Grid>
</Page>
