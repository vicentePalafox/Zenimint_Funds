<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Zenimint_Funds.Views.GastosPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Zenimint_Funds.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Zenimint_Funds.Models"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30" Spacing="15" Width="350" HorizontalAlignment="Left">
                <TextBlock Text="Registrar Gasto" FontSize="28" FontWeight="Bold"/>
                <InfoBar x:Name="Notificacion" IsOpen="False" />

                <TextBox x:Name="txtConcepto" Header="Concepto de la compra" PlaceholderText="Ej. Despensa, Celular..."/>
                <NumberBox x:Name="numMonto" Header="Monto Total ($)" PlaceholderText="0.00" Minimum="0.01"/>

                <TextBlock Text="Método de Pago" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <StackPanel Orientation="Horizontal" Spacing="15">
                    <RadioButton x:Name="optEfectivo" Content="Efectivo / Débito" IsChecked="True" Checked="MetodoPago_Changed"/>
                    <RadioButton x:Name="optTarjeta" Content="Tarjeta de Crédito" Checked="MetodoPago_Changed"/>
                </StackPanel>

                <ComboBox x:Name="cmbCategoriaGasto" 
          Header="Categoría del Gasto" 
          PlaceholderText="Selecciona una categoría..." 
          HorizontalAlignment="Stretch" 
          DisplayMemberPath="Nombre" 
          Margin="0,0,0,15" />

                <ComboBox x:Name="cmbTarjeta" Header="Selecciona tu Tarjeta" Visibility="Collapsed" 
                          HorizontalAlignment="Stretch" DisplayMemberPath="Nombre"/>

                <ToggleSwitch x:Name="tsMSI" Header="¿Es a Meses Sin Intereses?" Visibility="Collapsed" Toggled="tsMSI_Toggled"/>
                <NumberBox x:Name="numMeses" Header="Cantidad de Meses" Minimum="2" Maximum="24" Value="3" Visibility="Collapsed"/>

                <TextBlock Text="Asignación" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <ToggleSwitch x:Name="tsPrestamo" Header="¿Compraste esto para alguien más?" Toggled="tsPrestamo_Toggled"/>

                <StackPanel x:Name="panelDeudor" Visibility="Collapsed" Spacing="5">
                    <ComboBox x:Name="cmbDeudor" Header="Selecciona al Deudor" HorizontalAlignment="Stretch" DisplayMemberPath="Nombre"/>
                </StackPanel>

                <Button x:Name="btnGuardar" Content="Guardar Gasto" 
                        Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch"
                        Click="btnGuardar_Click" Margin="0,15,0,30"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Column="1" Margin="30,30,30,0" Spacing="15">
            <TextBlock Text="Últimos Gastos Registrados" FontSize="20" FontWeight="SemiBold" Foreground="Gray"/>
            <ListView x:Name="listaGastos" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Gasto">
                        <StackPanel Margin="0,10" Spacing="5">
                            <TextBlock Text="{x:Bind Concepto}" FontSize="18" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Spacing="15">

                                <TextBlock Foreground="Green">
                                    <Run Text=' $' />
                                    <Run Text="{x:Bind MontoFormateado}" />
                                </TextBlock>

                                <TextBlock Foreground="Gray" FontSize="12">
                                    <Run Text='Plazo:' />
                                    <Run Text="{x:Bind PlazoMeses}" />

                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

    </Grid>
</Page>
